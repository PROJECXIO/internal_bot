"""
Patch v0.0.3 — Add AI Schema Embedding Cache table.

The table itself is created automatically by `bench migrate` when it processes
the new DocType definition. This patch is a no-op placeholder that ensures the
patch is recorded in the migration log so it is not re-run.
"""


def execute():
    pass
